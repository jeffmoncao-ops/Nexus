[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q --strict-markers
markers =
    slow: testes longos (demos completas, suítes internas integrais)
    asyncio: testes assíncronos (executados pelo hook do conftest, sem plugin)
filterwarnings =
    ignore::DeprecationWarning
