[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q --strict-markers
markers =
    slow: testes longos (demos completas, suítes internas integrais)
filterwarnings =
    ignore::DeprecationWarning
asyncio_mode = auto
