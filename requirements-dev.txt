# ── NEXUS V14 UNIFIED — dependências de desenvolvimento/testes ────────────────
-r requirements-server.txt

pytest>=8.0        # suíte de testes (os testes async NÃO exigem pytest-asyncio)
httpx>=0.27        # usado pelo TestClient do FastAPI
